# C Algorithm Library

This directory will contain platform-independent C modules for:

- radar point definition
- filtering
- clustering
- tracking
- KPI statistics

The code in this directory should not depend directly on the TI mmWave SDK.
